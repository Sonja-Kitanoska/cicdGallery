import { expect, test } from "@jest/globals";
import {add } from './main'


// test("Creates a child element in main container", () => {
    
// }
// )


test("it adds two numbers", () => {
    expect(add(1,1)).toBe(2);
})


// test("Creates 10 child elements in main container")

// test("Child elements contain different images")

// test("Writes values to localStorage")

// test("Writes correct data to localStorage")

// test("Reads data from local storage")

// test("Renders data from local storage")
